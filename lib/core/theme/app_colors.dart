// ألوان حِرز الفاخرة / Hirz luxury palette
// كحلي، ذهبي، أخضر زيتي، أسود دافئ، فاتح عاجي
// Navy, gold, olive, warm black, off-white

import 'package:flutter/material.dart';

abstract class AppColors {
  // الكحلي / Navy
  static const Color navyPrimary = Color(0xFF0A1128);
  static const Color navySecondary = Color(0xFF0F1B33);

  // الذهبي / Gold
  static const Color goldPrimary = Color(0xFFC9A227);
  static const Color goldBright = Color(0xFFD4AF37);

  // الأخضر الزيتي / Olive
  static const Color olivePrimary = Color(0xFF6B8E23);
  static const Color oliveDeep = Color(0xFF556B2F);

  // الأسود الدافئ / Warm black (dark backgrounds)
  static const Color warmBlack = Color(0xFF12100E);

  // العاجي / Off-white (light backgrounds)
  static const Color offWhite = Color(0xFFF7F4EE);
}